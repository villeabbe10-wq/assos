{
  "hosting": {
    "site": "seducep",
    "public": "dist",
    "ignore": ["firebase.json", "**/.git/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  },
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
