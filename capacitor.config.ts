import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seduceconseil.assos',
  appName: 'SEDUCEP CONSEILS',
  webDir: 'dist'
};

export default config;
